# This file is intentionally committed.
# Only PUBLIC_* vars are exposed to the browser in Astro.
# Provide default public values for local preview/CI. Override using `.env` or hosting provider env.

# PostHog Project API key (leave empty or override in .env)
PUBLIC_POSTHOG_KEY=phc_3G37AiqLOZhmLNbAFHLNByyRfV1ooSHTBdJQ8guxPIf

# PostHog host (US or EU). Default is US cloud.
PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

