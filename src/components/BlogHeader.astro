---
import ResponsiveImage from './ResponsiveImage.astro';

interface Props {
  title: string;
  subtitle?: string;
  date: Date;
  heroImage?: string;
  readingTime?: number;
}

const { title, subtitle, date, heroImage, readingTime } = Astro.props;
---

<header class="relative mb-12 overflow-hidden rounded-2xl py-10 px-6 sm:py-14 lg:px-12">
  {/* Background with hero image or gradient */}
  {heroImage ? (
    <div class="absolute inset-0">
      <ResponsiveImage
        src={heroImage}
        alt=""
        class="h-full w-full object-cover"
        loading="eager"
        fetchpriority="high"
        sizes="(min-width: 768px) 768px, 100vw"
      />
      {/* Gradient overlay for readability */}
      <div class="absolute inset-0 bg-gradient-to-r from-[var(--color-bg-dark)]/95 via-[var(--color-bg-dark)]/80 to-[var(--color-bg-dark)]/60" />
      <div class="absolute inset-0 bg-gradient-to-t from-[var(--color-bg-dark)]/90 via-transparent to-transparent" />
    </div>
  ) : (
    <div class="absolute inset-0 bg-[var(--color-surface-dark)]">
      {/* Subtle gradient for posts without hero image */}
      <div
        class="absolute inset-0 opacity-30"
        style="background: radial-gradient(ellipse at top right, rgba(139, 92, 246, 0.15) 0%, transparent 60%);"
      />
    </div>
  )}

  {/* Content */}
  <div class="relative z-10 flex max-w-2xl flex-col items-start">
    {/* Meta info */}
    <div class="mb-6 flex flex-wrap items-center gap-4 text-xs font-medium uppercase tracking-[0.2em] text-[var(--color-muted-dark)]">
      <time datetime={date.toISOString()} class="flex items-center gap-2">
        <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
        </svg>
        {date.toLocaleDateString('en-US', {
          year: 'numeric',
          month: 'long',
          day: 'numeric',
        })}
      </time>
      {readingTime && (
        <>
          <span class="h-1 w-1 rounded-full bg-[var(--color-muted-dark)]" />
          <span class="flex items-center gap-2">
            <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            {readingTime} min read
          </span>
        </>
      )}
    </div>

    {/* Title */}
    <h1 class="display-heading text-4xl font-normal tracking-tight text-[var(--color-text-dark)] sm:text-5xl lg:text-5xl" style="line-height: 1.15;">
      {title}
    </h1>

    {/* Subtitle */}
    {subtitle && (
      <p class="mt-4 text-lg text-[var(--color-text-secondary-dark)] sm:text-xl" style="line-height: 1.6;">
        {subtitle}
      </p>
    )}
  </div>
</header>

<style>
  /* Light mode overrides */
  html.light header {
    background: var(--color-surface-light);
  }

  html.light header .absolute.inset-0:not(:has(img)) {
    background: var(--color-surface-light);
  }

  html.light header time,
  html.light header span {
    color: var(--color-muted-light);
  }

  html.light header h1 {
    color: var(--color-text-light);
  }

  html.light header p {
    color: var(--color-text-secondary-light);
  }
</style>
